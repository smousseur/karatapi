variables:
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"

cache:
  paths:
    - .m2/repository

stages:
  - build

build_java:
  image: maven:3.9.9-jdk-23
  stage: build
  script:
    - mvn clean install -DskipTests
    